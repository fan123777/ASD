#pragma once

namespace nsSorting
{
	namespace nsElementarySortingMethods
	{
		namespace nsArray
		{
			// Программа 6.7.Интерфейс для данных типа массив
			//template <class Item>
			//void rand(Item a[], int N);
			//template <class Item>
			//void scan(Item a[], int &N);
			//template <class Item>
			//void show(Item a[], int 1, int r);
			//template <class Item>
			//void sort(Item a[], int 1, int r);

			// Программа 6.8.Реализация типа данных массива
			/*template <class Item>
			void rand(Item a[], int N)
			{
				for (int i = 0; i < N; i++)
					std::rand(a[i]);
			}*/

			/*template <class Item>
			void scan(Item a[], int& N)
			{
				for (int i = 0; i < N; i++)
					if (!scan(a[i]) break;
				N = i;
			}*/

			/*template <class Item>
			void show(Item a[], int 1, int r)
			{
				for (int i = 1; i <= r;i++)
					show(a[i]);
				std::cout << std::endl;
			}*/
		}
	}
}
